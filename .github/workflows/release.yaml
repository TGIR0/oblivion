name: Release candidate

on:
  workflow_dispatch:
  release:
    types: ["created"]

jobs:
  build:
    name: Build & Release
    permissions:
      contents: read
      id-token: write
      attestations: write
      artifact-metadata: write
    runs-on: ubuntu-latest

    env:
      CGO_ENABLED: 1

    steps:
      - name: Checkout codebase
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Setup Go
        uses: actions/setup-go@924ae3a1cded613372ab5595356fb5720e22ba16 # v6
        with:
          go-version: "1.26.4"
          cache: true

      - name: Setup JDK
        uses: actions/setup-java@1bcf9fb12cf4aa7d266a90ae39939e61372fe520 # v5
        with:
          java-version: "26"
          distribution: "temurin"
          cache: "gradle"

      - name: Setup Gradle Cache
        uses: gradle/actions/setup-gradle@0723195856401067f7a2779048b490ace7a47d7c # v5

      - name: Setup Android SDK
        uses: android-actions/setup-android@9fc6c4e9069bf8d3d10b2204b1fb8f6ef7065407 # v3

      - name: Install native build tools
        run: sdkmanager "platforms;android-37" "build-tools;37.0.0" "ndk;30.0.14904198"

      - name: Grant Execute Permission to Gradlew
        run: chmod +x ./gradlew

      - name: Install gomobile
        run: go install golang.org/x/mobile/cmd/gomobile@v0.0.0-20260611195102-4dd8f1dbf5d2

      - name: Install native SBOM generator
        run: go install github.com/CycloneDX/cyclonedx-gomod/cmd/cyclonedx-gomod@v1.10.0

      - name: Configure release signing
        if: github.event_name == 'release'
        env:
          ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
        run: |
          set -euo pipefail
          : "${ANDROID_KEYSTORE_BASE64:?ANDROID_KEYSTORE_BASE64 is required}"
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 --decode > release-keystore.jks

      - name: Build release candidate and Android SBOM
        env:
          ANDROID_KEYSTORE_FILE: ${{ github.event_name == 'release' && 'release-keystore.jks' || '' }}
          ANDROID_KEYSTORE_PASSWORD: ${{ github.event_name == 'release' && secrets.ANDROID_KEYSTORE_PASSWORD || '' }}
          ANDROID_KEY_ALIAS: ${{ github.event_name == 'release' && secrets.ANDROID_KEY_ALIAS || '' }}
          ANDROID_KEY_PASSWORD: ${{ github.event_name == 'release' && secrets.ANDROID_KEY_PASSWORD || '' }}
        run: |
          ./gradlew \
            spotlessCheck \
            :app:testDebugUnitTest \
            :app:assembleRelease \
            :app:lintRelease \
            :app:cyclonedxDirectBom \
            --warning-mode all \
            --no-daemon

      - name: Generate native SBOM
        working-directory: tun2socks
        run: |
          mkdir -p ../build/reports/sbom
          cyclonedx-gomod mod \
            -json \
            -type library \
            -output ../build/reports/sbom/native-bom.cdx.json \
            .

      - name: Verify release signature
        if: github.event_name == 'release'
        run: |
          "$ANDROID_HOME/build-tools/37.0.0/apksigner" verify \
            --verbose \
            --print-certs \
            app/build/outputs/apk/release/*.apk

      - name: Checksums
        run: |
          sha256sum \
            app/libs/tun2socks.aar \
            app/build/outputs/apk/release/*.apk \
            app/build/reports/cyclonedx-direct/bom.json \
            app/build/reports/cyclonedx-direct/bom.xml \
            build/reports/sbom/native-bom.cdx.json \
            > SHA256SUMS

      - name: Attest build provenance
        uses: actions/attest@a1948c3f048ba23858d222213b7c278aabede763 # v4
        with:
          subject-path: |
            app/libs/tun2socks.aar
            app/build/outputs/apk/release/*.apk

      - name: Attest Android SBOM
        uses: actions/attest@a1948c3f048ba23858d222213b7c278aabede763 # v4
        with:
          subject-path: app/build/outputs/apk/release/*.apk
          sbom-path: app/build/reports/cyclonedx-direct/bom.json

      - name: Attest native SBOM
        uses: actions/attest@a1948c3f048ba23858d222213b7c278aabede763 # v4
        with:
          subject-path: app/libs/tun2socks.aar
          sbom-path: build/reports/sbom/native-bom.cdx.json

      - name: Upload Tun2Socks AAR
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: tun2socks-aar
          path: app/libs/tun2socks.aar
          if-no-files-found: error

      - name: Upload Release APK
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: oblivion-release-${{ github.event_name == 'release' && 'signed' || 'unsigned' }}
          path: app/build/outputs/apk/release/*.apk
          if-no-files-found: error

      - name: Upload checksums
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: checksums
          path: SHA256SUMS

      - name: Upload SBOMs
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: cyclonedx-sboms
          path: |
            app/build/reports/cyclonedx-direct/bom.json
            app/build/reports/cyclonedx-direct/bom.xml
            build/reports/sbom/native-bom.cdx.json
          if-no-files-found: error

      - name: Remove signing material
        if: always() && github.event_name == 'release'
        run: rm -f release-keystore.jks
